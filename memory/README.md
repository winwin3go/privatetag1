# Spec Kit Memory

This folder stores generated artifacts (e.g., clarifications, scratch notes) produced by `/speckit.*` commands. Leave this directory in git so agents can persist context across sessions.
