# Specs Index

Store every feature under `specs/<id>-<slug>/` with:

- `spec.md`
- `plan.md`
- `tasks.md`
- `checklists/requirements.md`

IDs are zero-padded (e.g., `001`, `002`). Create folders via `pnpm speckit:new-feature`.
